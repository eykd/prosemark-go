# Project State Summary — 2026-03-10

## Executive Summary

`prosemark-go` is a Go CLI tool (`pmk`) for managing long-form prose projects using a binder-based outline strategy. As of this date, four major features have been fully implemented and merged to `master` — **001-prosemark-binder** (core parse/add/delete/move operations), **002-node-identity** (project init, node creation, editor integration, integrity validation), **003-smoke-test-bugs** (CLI output correctness, error propagation fixes), and **003-placeholder-parsing** (placeholder node recognition, BNDW006 diagnostic, conformance fixtures 121–125). The codebase passes all quality gates with 97.8% overall test coverage (100% of non-Impl functions covered) and hosts 31 source files across 7 packages, backed by 77 test files.

---

## Project Identity

| Field | Value |
|-------|-------|
| Module | `github.com/eykd/prosemark-go` |
| Binary | `pmk` |
| Go Version | `1.25.6` |
| Direct Dependencies | `github.com/google/uuid v1.6.0`, `github.com/spf13/cobra v1.10.2`, `gopkg.in/yaml.v3 v3.0.1` |
| Indirect Dependencies | `github.com/inconshreveable/mousetrap v1.1.0`, `github.com/spf13/pflag v1.0.9` |
| Current Branch | `master` |
| Active Feature | None (on master; last feature `003-placeholder-parsing` merged via PR #5 ~2026-03-10) |

---

## Architecture Overview

```
prosemark-go/
├── main.go                               # Entry point; version injection
├── cmd/                                  # Cobra subcommands (CLI layer)
│   ├── root.go                           # Root command; project dir resolution, flag validation, diagnostic output
│   ├── parse.go                          # pmk parse → JSON to stdout
│   ├── addchild.go                       # pmk add-child → confirmation to stdout; --new mode for node creation
│   ├── delete.go                         # pmk delete → confirmation to stdout
│   ├── move.go                           # pmk move → confirmation to stdout
│   ├── init.go                           # pmk init → initialize new project
│   ├── edit.go                           # pmk edit → open node file in $EDITOR
│   ├── doctor.go                         # pmk doctor → project integrity validation
│   ├── diagnostics.go                    # Diagnostic code registry and helpers
│   ├── scan.go                           # Filesystem scan for .md files (Impl pattern)
│   ├── sanitize.go                       # Filename sanitization helpers
│   └── binder_lock.go                    # Concurrent binder access locking
├── internal/binder/                      # Domain layer: binder format
│   ├── types.go                          # Node, Diagnostic, ParseResult, OpSpec, OpResult, error/warning codes
│   ├── parser.go                         # Two-pass binder parser (wikilinks, refs, fences, placeholders)
│   ├── selector.go                       # Selector evaluation (path:segment[N] syntax)
│   ├── serializer.go                     # Lossless round-trip serialization
│   └── ops/                              # Mutation operations
│       ├── addchild.go                   # Insert child node at position
│       ├── delete.go                     # Remove node and subtree
│       └── move.go                       # Relocate node with re-indentation
├── internal/node/                        # Domain layer: node identity
│   ├── types.go                          # NodeId, Frontmatter, NodePart, AuditCode, AuditSeverity
│   ├── frontmatter.go                    # Parse/validate YAML frontmatter blocks
│   ├── validate.go                       # Validate node file structure and references
│   ├── doctor.go                         # Audit project: missing/orphaned files, duplicates, ID mismatches
│   ├── collect_binder_refs.go            # Collect file references from parsed binder structure
│   └── nowutc.go                         # RFC3339 timestamp generation
├── acceptance/                           # ATDD pipeline infrastructure
│   ├── types.go                          # Step, Scenario, Feature types
│   ├── parser.go                         # GWT spec parser
│   ├── generator.go                      # Test code generator (preserves bound tests)
│   ├── ir.go                             # Intermediate representation (JSON)
│   └── cmd/pipeline/main.go              # Pipeline CLI (parse|generate|run)
├── conformance/                          # Binary conformance test runner
│   └── runner_test.go
├── generated-acceptance-tests/           # Auto-generated from specs (committed)
├── acceptance-pipeline/ir/               # Parsed IR JSON (committed)
├── specs/001-prosemark-binder/           # Feature 001 GWT specs and design docs
│   ├── spec.md, plan.md, data-model.md, research.md, quickstart.md, requirements.md
│   ├── contracts/                        # Contract documents
│   ├── US1-parse-binder-file.txt         # 9 GWT scenarios
│   ├── US2-add-child-node.txt            # 9 GWT scenarios
│   ├── US3-delete-node.txt               # 9 GWT scenarios
│   ├── US4-move-node.txt                 # 7 GWT scenarios
│   └── US5-conformance-test-suite.txt    # 6 GWT scenarios
├── specs/002-node-identity/              # Feature 002 GWT specs and design docs
│   ├── spec.md, plan.md, data-model.md, research.md, requirements.md
│   ├── contracts/                        # commands.md, node-frontmatter.md
│   ├── US1-init-project.txt
│   ├── US2-create-new-node.txt
│   ├── US3-edit-node.txt
│   └── US4-validate-integrity.txt
├── specs/003-placeholder-parsing/        # Feature 003 GWT specs
│   ├── spec.md, plan.md
│   ├── US1-recognize-placeholder-nodes.txt  # 3 scenarios
│   ├── US2-placeholder-nodes-with-children.txt  # 2 scenarios
│   ├── US3-placeholder-round-trip.txt    # 1 scenario
│   └── US4-no-spurious-diagnostics.txt   # 2 scenarios
└── docs/
    ├── prosemark_binder_format_spec_v1.md      # Normative binder format spec
    ├── prosemark_binder_operations_spec_v1.md  # Normative operations spec
    ├── prosemark_cli_prd.md                    # Product requirements document
    ├── plan/                                   # Feature planning docs (01-05)
    ├── conformance/                            # Conformance suite overview + v1 fixtures/schemas
    └── summaries/                              # Project state snapshots
```

---

## CLI Interface

All subcommands accept `--project <dir>` to specify the project directory; defaults to `$CWD`. The binder file is always `_binder.md` within that directory.

| Command | Flags | Output |
|---------|-------|--------|
| `pmk parse` | `--project <dir>` | JSON to stdout |
| `pmk add-child` | `--project`, `--parent`, `--target`, `--title`, `--first/--at/--before/--after`, `--force`, `--new` | Confirmation to stdout; diagnostics to stderr |
| `pmk delete` | `--project`, `--selector`, `--yes` | Confirmation to stdout; diagnostics to stderr |
| `pmk move` | `--project`, `--source`, `--dest`, `--yes`, `--first/--at/--before/--after` | Confirmation to stdout; diagnostics to stderr |
| `pmk init` | `--project` | Initializes `_binder.md` and `.prosemark.yml` |
| `pmk edit` | `--project`, `--selector` | Opens node file in `$EDITOR` |
| `pmk doctor` | `--project` | Audit diagnostics to stdout/stderr |

**Output convention**: Human-readable confirmations go to stdout; diagnostic messages (errors and warnings with codes) go to stderr. Exit 1 on any error diagnostic.

**`--json` flag removed**: As of the 003-smoke-test-bugs sprint, all commands emit human-readable output by default. The conformance runner uses `binder.OpResult` (structured JSON type) rather than a `--json` CLI flag.

---

## Domain Model

### Core Types

| Type | Package | Key Fields |
|------|---------|-----------|
| `Node` | `internal/binder` | `Type` ("root"\|"node"), `Target` (resolved path), `Title`, `Children` (ordered, never nil), source metadata: `Line`, `Column`, `ByteOffset`, `EndLine`, `SubtreeEnd`, `Indent`, `IndentChar`, `ListMarker`, `RawLine`, `InCodeFence` |
| `Diagnostic` | `internal/binder` | `Severity` ("error"\|"warning"), `Code` (e.g. "BNDE001"), `Message`, `Location` (line/col/byte, optional) |
| `ParseResult` | `internal/binder` | `Version` ("1"), `Root` (*Node), `Lines` (original source), `LineEnds`, `RefDefs`, `HasBOM`, `HasPragma`, `PragmaLine` |
| `OpResult` | `internal/binder` | `Version` ("1"), `Changed` (bool), `Diagnostics` ([]Diagnostic) |
| `NodeId` | `internal/node` | string alias (UUID v7) |
| `Frontmatter` | `internal/node` | `ID` (UUID v7), `Title`, `Synopsis` (optional strings), `Created`, `Updated` (RFC3339) |
| `AuditCode` | `internal/node` | AUD001–AUD008, AUDW001, BNDW001 |

### Selector Syntax

Selectors navigate the binder tree:
- Bare stem: `chapter` matches `chapter.md`
- Index qualifier: `chapter[1]` picks the second match
- Chain: `part1:chapter` navigates to child of `part1`
- Root: `.` refers to the binder root

### Diagnostic Codes

| Category | Codes | Severity | Trigger |
|----------|-------|----------|---------|
| Parse errors | `BNDE001`–`BNDE003` | error (exit 1) | Illegal path chars, path escapes root, ambiguous wikilink |
| Parse warnings | `BNDW001`–`BNDW010` | warning (exit 0) | Missing pragma, various lint warnings |
| Operation errors | `OPE001`–`OPE010` | error (exit 1) | No match, ambiguous selector, cycle, invalid target, target is binder, node in fence, sibling not found, index OOB, I/O failure, conflicting flags |
| Operation warnings | `OPW001`–`OPW005` | warning (exit 0) | Multi-match, duplicate skipped, non-structural content destroyed, empty sublist pruned, cascade delete |
| Audit errors | `AUD001`–`AUD008` | error | Missing file, orphaned node, duplicate ref, ID mismatch, missing frontmatter, empty file, unparseable YAML, missing/invalid config |
| Audit warnings | `AUDW001`, `BNDW001` | warning | Non-UUID filename, binder missing pragma |

---

## Feature Status

### Completed Features

| Feature | Branch | PR | Merged | Description |
|---------|--------|----|--------|-------------|
| 001-prosemark-binder | `001-prosemark-binder` | #1 | 2026-02-24 | Core parse/add-child/delete/move operations; conformance suite |
| 002-node-identity | `002-node-identity` | #3 | 2026-02-27 | Project init, node creation with UUID v7 frontmatter, editor integration, integrity validation |
| 003-smoke-test-bugs | `003-smoke-test-bugs` | #4 | 2026-03-02 | CLI output correctness: OpResult refactor, error propagation fixes, multiset merge, noop move suppression |
| 003-placeholder-parsing | `003-placeholder-parsing` | #5 | ~2026-03-10 | Placeholder node recognition, children, round-trip, BNDW006 diagnostic, conformance fixtures 121–125 |

### Acceptance Specs

| Feature | User Story | Scenarios | Description |
|---------|-----------|-----------|-------------|
| 001 | US1-parse-binder-file | 9 | Inline/wiki/footnote links, error codes, code blocks, CRLF |
| 001 | US2-add-child-node | 9 | Positioning flags, idempotency, ordered lists, tab indent |
| 001 | US3-delete-node | 9 | Leaf/subtree, blank line collapse, footnote preservation |
| 001 | US4-move-node | 7 | Reorder, subtree, cycle detection, re-indentation |
| 001 | US5-conformance-test-suite | 6 | All conformance fixtures pass |
| 002 | US1-init-project | — | Initialize `_binder.md` and `.prosemark.yml` |
| 002 | US2-create-new-node | — | Create `{uuid}.md` with frontmatter, add to binder |
| 002 | US3-edit-node | — | Open node file in `$EDITOR` |
| 002 | US4-validate-integrity | — | Audit: missing files, orphans, ID mismatches, malformed frontmatter |
| 003 | US1-recognize-placeholder-nodes | 3 | Placeholder link detection on list items |
| 003 | US2-placeholder-nodes-with-children | 2 | Placeholders as parents with children |
| 003 | US3-placeholder-round-trip | 1 | Lossless serialization of placeholders |
| 003 | US4-no-spurious-diagnostics | 2 | No false positives for placeholder syntax |

---

## Test Infrastructure

### Test Streams

| Stream | Purpose | Command | Status |
|--------|---------|---------|--------|
| Unit tests | Internal correctness | `just test` | PASS |
| Coverage check | 100% non-Impl coverage | `just test-cover-check` | PASS (97.8% overall) |
| Conformance tests | Binary-level behavior | `just conformance-run` | PASS |
| Acceptance tests | Observable behavior | `just acceptance` | Generated; bound stubs present |

### Coverage by Package

| Package | Coverage | Non-Impl 100%? |
|---------|----------|----------------|
| `internal/binder` | 100.0% | Yes |
| `internal/binder/ops` | 100.0% | Yes |
| `internal/node` | 100.0% | Yes |
| `acceptance` | 92.0% | Yes (Impl functions exempt) |
| `cmd` | 95.7% | Yes (Impl functions exempt) |
| `conformance` | (no statements) | N/A |
| **Overall** | **97.8%** | **Yes** |

### Impl Pattern

Functions wrapping OS/exec calls are named with `*Impl` suffix and excluded from coverage calculation:
- `WriteBinderAtomicImpl` — atomic temp-file-then-rename write
- `ScanProjectImpl` — filesystem scan for project `.md` files
- `OpenEditorImpl` — launch `$EDITOR` for node editing
- `ParseSpecFileImpl` — disk read for acceptance spec parsing
- `WriteTestFileImpl`, `WriteIRImpl`, `ReadIRImpl` — acceptance pipeline I/O
- `readBinderSizeLimitedImpl` — binder file read with size limit

These functions are exercised by integration and acceptance tests, not unit tests.

### Test File Counts by Package

| Package | Test Files |
|---------|-----------|
| `cmd/` | 39 |
| `generated-acceptance-tests/` | 14 |
| `internal/binder/ops/` | 7 |
| `internal/node/` | 7 |
| `internal/binder/` | 5 |
| `acceptance/` | 4 |
| `conformance/` | 1 |
| **Total** | **77** |

---

## Quality Gates

All enforced via `lefthook` pre-commit hooks and `just check`:

| Gate | Tool | Status |
|------|------|--------|
| Formatting | `gofmt` | Pass |
| Static analysis | `go vet` | Pass |
| Linting | `staticcheck` | Pass |
| Coverage | 100% non-Impl | Pass |
| CLI smoke test | `just smoke` | Pass |

---

## Beads Issue Tracker

**Epics**: 0 open
**Open bugs**: 0

The beads database is currently empty — all previous epics and tasks have been completed and closed. The 003-placeholder-parsing sprint (PR #5) was the last recorded work unit.

---

## ATDD Pipeline

```
specs/001-prosemark-binder/*.txt
specs/002-node-identity/*.txt
specs/003-placeholder-parsing/*.txt
    │ (just acceptance-parse)
    ▼
acceptance-pipeline/ir/*.json        ← committed intermediate representation
    │ (just acceptance-generate)
    ▼
generated-acceptance-tests/*_test.go ← committed generated test stubs
    │ (just acceptance-run)
    ▼
Test results (Go test output)
```

**Stub preservation**: The generator (`acceptance/generator.go`) detects bound (implemented) test functions and preserves their bodies across regeneration — only unbound stubs (containing `t.Skip("acceptance test not yet bound")`) are overwritten.

**Pipeline CLI**: `go run ./acceptance/cmd/pipeline -action=<parse|generate|run>`

---

## Normative Specifications

| File | Description |
|------|-------------|
| `docs/prosemark_binder_format_spec_v1.md` | Normative binder format: `_binder.md` identification, UTF-8, pragma, structural link forms (inline/wikilink/footnote), round-trip fidelity requirement |
| `docs/prosemark_binder_operations_spec_v1.md` | Normative operations: error/warning code schema (`BNDE*`, `BNDW*`, `OPE*`, `OPW*`), operation preconditions/postconditions, conformance fixture format and runner contract |
| `docs/prosemark_cli_prd.md` | Product requirements document for CLI |
| `docs/conformance/README.md` | Conformance suite overview: how to add fixtures, runner contract |
| `docs/conformance/v1/` | JSON fixture files and JSON schemas for binary-level conformance testing |
| `docs/plan/01-placeholder-parsing.md` | Placeholder syntax feature plan |
| `docs/plan/02-structure.md` | Project structure planning |
| `docs/plan/03-notes-edit.md` | Notes editing feature plan |
| `docs/plan/04-materialize.md` | Materialization strategy |
| `docs/plan/05-compile.md` | Compilation strategy |

---

## Development Workflow

### Standard TDD Cycle

1. Write failing test (Red)
2. Write minimal implementation to pass (Green)
3. Refactor while keeping tests green

### Quality Check Sequence

```bash
just check           # fmt-check + vet + lint + test-cover-check (all quality gates)
just smoke           # CLI smoke test (required after changes to cmd/ or main.go)
just test-all        # unit tests + acceptance tests + conformance tests
```

### Commit Convention

Conventional commits (`feat`, `fix`, `refactor`, `test`, `chore`) with scope in parentheses (e.g. `fix(binder):`).
All commits co-authored with `Claude Sonnet 4.6`.

---

## Skills Registry

| Skill | Purpose |
|-------|---------|
| `acceptance-tests` | Generate and manage GWT acceptance test stubs |
| `atdd` | Guide acceptance test-driven development workflow |
| `beads-batch-ops` | Batch operations against the Beads issue tracker |
| `clean-architecture-validator` | Validate layer boundaries and dependency direction |
| `commit` | Create conventional commits with co-author attribution |
| `ddd-domain-modeling` | Domain-driven design modeling and type design |
| `go-cli-ddd` | Go CLI architecture with DDD layering (cmd/internal separation) |
| `go-property-testing` | Property-based testing with rapid library |
| `go-tdd` | Go-specific TDD patterns, table-driven tests, coverage exemptions |
| `portable-datetime` | Cross-platform UTC timestamp generation |
| `prefactoring` | Identify and prepare for refactoring before new features |
| `quality-review` | Run and interpret all quality gates |
| `refactoring` | Safe, incremental refactoring with continuous test coverage |
| `security-review` | Audit code for security vulnerabilities |
| `skill-creator` | Create new reusable skill documents |
| `skill-improver` | Improve and update existing skill documents |
| `summary` | Generate project state snapshot documents |

---

## Changes Since Last Snapshot (2026-03-03)

1. **New feature merged**: `003-placeholder-parsing` (PR #5) — placeholder node recognition in binder parser, BNDW006 diagnostic for placeholder links on non-list lines, conformance fixtures 121–125, and 4 new GWT acceptance spec files (8 total scenarios)
2. **Coverage improved**: 97.1% → 97.8% overall; `cmd` package 91.8% → 95.7%; `internal/node` — → 100.0%
3. **Test files grew**: 73 → 77 unit/acceptance test files; generated acceptance tests 10 → 14
4. **Parser updated**: `internal/binder/parser.go` now handles placeholder nodes (wikilinks, refs, fences, and placeholders — updated comment in architecture tree)
5. **Beads database cleared**: All previous epics/tasks completed and closed (was 3 open epics + 1 in-progress bug)
6. **New skill added**: `go-property-testing` — property-based testing with rapid library (skills count 16 → 17)
7. **New spec directory**: `specs/003-placeholder-parsing/` with `spec.md`, `plan.md`, and 4 US spec files
